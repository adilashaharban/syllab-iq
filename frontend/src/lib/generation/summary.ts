export function generateGroundedSummary(retrievedContext: string): string {
  // Simulates generation of a study guide summary strictly grounded in context
  return `This summary is compiled directly from the approved syllabus documents. Key definitions include process allocation mechanisms, Mutual Exclusion, and Resource Allocation Graph structures.`;
}
