export function generateGroundedTakeaways(retrievedContext: string): string[] {
  // Simulates generation of key takeaways grounded in context
  return [
    "Deadlock occurs when four conditions hold simultaneously: Mutual Exclusion, Hold & Wait, No Preemption, and Circular Wait.",
    "A cycle in a Resource Allocation Graph guarantees a deadlock only if resources have a single unit instance."
  ];
}
